# MemPalace MCP Server — Databricks App dependencies
# This file is used by the Databricks App runtime (overrides pyproject.toml).
databricks-sdk>=0.40.0
databricks-vectorsearch>=0.40
mcp>=1.0.0
uvicorn>=0.30.0
pyyaml>=6.0,<7
